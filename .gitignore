# Build outputs
**/build/
**/.cxx/
**/.gradle/
*.apk
*.so
*.o
*.a
build64/
efsp_obb_backup/

# Local config
**/local.properties

# Release signing key (NEVER commit — debug.keystore is intentionally tracked)
*.jks
voyagersp-release.keystore
.idea/
*.iml

# Game data / binaries (not redistributable; not source)
*.pk3
*.exe
*.dll
*.bik
*.roq

# Logs / scratch
*.log
/tmp/
efsp_*.log
*.sav
_save_backup_*/
*.idsig

# Local-only analysis artifacts (not source, never publish)
binary_analysis/

# Vendored third-party deps (not our source)
thirdparty/
*.DLL
*.EXE
