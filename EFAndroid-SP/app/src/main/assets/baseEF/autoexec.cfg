// ===== Voyager EF — Android autoexec =====
// INTENTIONALLY EMPTY of seta/bind lines: autoexec.cfg executes on EVERY boot
// AFTER efspconfig.cfg, so anything here would clobber settings the user changed
// in the menus (video options, aspect, controller rebinds...).
// First-boot defaults live in android_defaults.cfg, applied exactly once by
// the engine (Com_ExecuteCfg, in_androidDefaults guard).
